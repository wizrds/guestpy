version: '3'

tasks:
  update:
    cmds: 
      - cargo update

  check:
    desc: Check the code
    cmds:
      - cargo check --all-features --tests {{.CLI_ARGS}}

  fmt:
    desc: Format the code
    cmds:
      - >
        cargo fmt --all -- --check

  fmt:fix:
    desc: Format the code
    cmds:
      - >
        cargo fmt --all

  lint:
    desc: Lint the code
    cmds:
      - >
        cargo clippy --all-targets --all-features {{.CLI_ARGS}}

  lint:fix:
    desc: Lint the code
    cmds:
      - >
        cargo clippy --fix --all-targets --all-features --allow-dirty --allow-staged {{.CLI_ARGS}}

  test:
    cmds:
      # Unit tests require release due to recursion limit in debug builds
      - cargo test --all-features --release {{.CLI_ARGS}}

  audit:
    cmds:
      - cargo audit {{.CLI_ARGS}}

  bench:
    cmds:
      - cargo bench --all-features {{.CLI_ARGS}}

  bench:criterion:
    cmds:
      - cargo criterion --all-features {{.CLI_ARGS}}

  build:
    desc: Build the application
    cmds:
      - cargo build --all-features {{.CLI_ARGS}}
